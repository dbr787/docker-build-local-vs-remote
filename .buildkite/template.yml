name: "Docker Build Local vs Remote Benchmark"
description: "Benchmark Docker build performance between local and remote builders"
steps:
  - label: ":pipeline:"
    command: "buildkite-agent pipeline upload"
